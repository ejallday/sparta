= "#{Student.human_attribute_name(:first_name)}: #{student.first_name}"
= "#{Student.human_attribute_name(:last_name)}: #{student.last_name}"
= "#{Student.human_attribute_name(:email)}: #{student.email}"
= "#{Student.human_attribute_name(:username)}: #{student.username}"
